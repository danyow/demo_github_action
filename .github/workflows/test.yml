name: 查看仓库名字
on:
  workflow_dispatch: { }
jobs:
  activation:
    name: 查看仓库名字 🔑
    runs-on: ubuntu-latest
    steps:
      - name: 查看仓库名字
        run: |
          echo ${{github.event.repository.name}}